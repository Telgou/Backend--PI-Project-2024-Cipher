/*import mongoose from "mongoose";
import User from "./User";

const Admin = User.discriminator('admin', new mongoose.Schema({
    role: {
        type: String,

        enum: ['admin', 'coordinator', 'dephead'],

        default: 'admin',
    },
    //adminPrivileges: [String]
}));

export default Admin;*/
